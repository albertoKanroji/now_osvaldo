import 'package:flutter/material.dart';

class CodigoView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Código')),
      body: Center(child: Text('Vista de Código')),
    );
  }
}
