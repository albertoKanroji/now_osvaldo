import 'package:flutter/material.dart';

class MistadosView extends StatelessWidget {
  const MistadosView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Mis Datos')),
      body: Center(child: Text('Vista de Más')),
    );
  }
}
